import { defineConfig } from "vite";

export default defineConfig({
  base: "/slidev-dou-day-2025-ai-dev-done-right/",
  // Optional: Add server fs allow if needed, but start without it
  // server: {
  //   fs: {
  //     allow: ['..']
  //   }
  // }
});
